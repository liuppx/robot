# Messenger runtime settings
#
# This file is a template only.
# Copy the values you need into your local environment or a local env file
# that is not committed.

# Default runtime root for message bot instances.
MESSENGER_INSTANCES_ROOT=runtime/instances

# Default provider used when creating new instances from the hub.
MESSENGER_DEFAULT_MODEL=gpt-5.3-codex

# Optional admin token used by hub management endpoints.
MESSENGER_ADMIN_TOKEN=
